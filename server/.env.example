PORT=3001
MONGODB_URI=mongodb://localhost:27017/solar_estimator
# Add any optional keys below (e.g. OpenAI keys if using a third-party LLM,
# but the app implements a robust local fallback chatbot that runs out of the box)
